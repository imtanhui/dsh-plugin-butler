# dsh bundle patch: inserts this plugin's Host entry into a profile's layer stack.
# The Client half is wired separately through package.json's `dsh.client` manifest.
- insert:
    - id: dsh-plugin-manager
      name: 'dsh-plugin-manager'
